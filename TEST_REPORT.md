# 검증 결과

검증일: **2026-09-19**. 로컬 개발 서버와 빌드한 Worker를 사용했다. 공개 배포·업로드·계정 결제 변경은 수행하지 않았다.

## 최종 결과

| 검사                                 | 결과                                        |
| ------------------------------------ | ------------------------------------------- |
| TypeScript / ESLint / 빌드           | 모두 통과                                   |
| 기존 도메인·제공자·키 진단·시작 경로 | 66개 통과                                   |
| 새 추천 흐름 회귀 검사               | 10개 통과                                   |
| 일반 HTTP 통합                       | 11개 통과                                   |
| 실제 API 통합                        | Kakao·식약처·OpenRouter·행정안전부 4개 통과 |
| 전체 Node 검사                       | **91개 통과, 실패 0, 미실행 0**             |
| 최종 빌드의 Chromium 검사            | **11개 통과, 실패 0**                       |
| 클라이언트 키 검사                   | 빌드 자산 23개, 실제 키 항목 4개, 검출 0    |
| 사용자용 개발 서버                   | 5173 응답, 기본 live, Kakao 설정 확인       |
| Git 제외                             | .env 및 진단 출력 제외 확인                 |

Node 전체 검사는 빌드된 Worker를 대상으로 실행했다. 이후 화면의 간단히 보기·모바일 입력 폭을 다듬고 타입·린트·빌드 및 전체 Chromium 11개를 다시 확인했다. 반복 실행 횟수를 별도 시나리오로 더하지 않았다.

## 사용자 화면 문제의 재현과 해결

- **예산 25,000원 / 반경 15m / 시간 60분:** 시연 결과 0을 재현했다. 예산을 올리는 무관한 제안을 제거하고, 후보 3곳이 추가되는 700m 반경만 제시한다. 클릭하면 확인이 필요한 후보 3곳이 표시된다. 원래 예산은 유지한다.
- **실제 모드 기본값:** 서버가 DEMO_MODE=false이면 화면도 실제 식당 찾기로 시작한다. 임의의 시연 위치로 실제 요청을 보내지 않는다. 실제 위치는 기록 질문보다 먼저 선택한다.
- **실제 서울역 전체 흐름:** 새 브라우저에서 동의·연령 설정 → 역 이름 검색 → 검색 결과 선택 → 한식·반경 1km 검색 → 실제 식당 15곳 중 3곳 표시 → 상세·길찾기 링크 확인 → 방문할 식당 선택 → 실제 먹은 음식 입력·저장까지 통과했다. 식당 선택만으로는 기록이 생기지 않는다. 실제 Kakao 호출을 사용했고 사용자 위치를 읽지 않았다.
- **거리 단위:** 15m에서 표시 단위를 km로 바꾸면 0.015km로 보인다. 1km 선택 시 서버에는 1,000m 조건이 전달된다. 좁은 반경 안내와 모바일 입력 폭을 확인했다.
- **조건 변경의 근거:** 실제 후보가 늘어나는 경우만 변경안을 발급한다. 넓힌 범위에도 후보가 없으면 제안하지 않는다. 미확인 가격 때문에 예산을 올리지 않는다.
- **요청 연속성:** 위치 선택·어제 질문·조건 변경에서 그룹을 유지한다. 서버는 이전 요청의 위치·예산·그룹이 바뀐 변경안을 409로 거절한다. 정확한 위치를 추천 캐시에 저장하지 않는다.
- **제한사항:** 명시적 제외와 알려진 휴·폐업·거리 위반은 식당 후보에서도 제외한다. 심한 알레르기 또는 미량 민감성은 안전한 메뉴 추천으로 표시하지 않으며 확인용 장소로만 보여준다.

실제 식당 후보는 메뉴·가격·성분이 모두 검증된 메뉴 추천과 다르다. 가격·영업·포장·소요시간 등의 확인 필요 항목을 카드에 남기고, 메뉴·가격 보기 및 길찾기로 연결한다. 일반 식품 DB를 특정 식당 메뉴 근거로 사용하지 않는다.

## 기존 기능 회귀 확인

메뉴 추천·지도 핀·선택·섭취 확인·평가·기록 수정·삭제, 모바일 메뉴, 14세 미만 둘러보기, 위치 권한 거부 후 직접 입력, 그룹 동행인 추가·삭제, 공식 식품의 g 기준 영양량, AI 동의·검토·적용을 검증했다. 실제 OpenRouter 생성은 HTTP 검사에서 별도로 수행하고, 브라우저 AI 미리보기 검사는 고정 응답을 사용한다.

실제 API HTTP 검사 및 새 브라우저 검사는 별도 합성 세션을 사용하고 finally/afterEach에서 삭제한다. 사용자의 실제 세션 설정·식사 기록·정확한 위치를 테스트에 사용하지 않았다.

## 화면 확인

- [실제 식당 데스크톱](docs/screenshots/live-restaurants-desktop.png)
- [실제 식당 모바일](docs/screenshots/live-restaurants-mobile.png)
- [공식 식품 데스크톱](docs/screenshots/official-food-desktop.png)
- [공식 식품 모바일](docs/screenshots/official-food-mobile.png)

Chromium에서 캡처했고 가로 넘침·버튼·단위 입력을 확인했다. Windows 이미지 뷰어 접근 문제는 메모리의 읽기 전용 이미지 로딩으로 대체했다.

## 검증 중 정리한 사항

- 이전 테스트의 ‘위치 선택’ 문구가 새 안내 버튼과 겹쳐 접근성 선택자를 정확한 이름으로 수정했다. 위치를 선택하면 설정으로 이어지는 흐름에 맞게 다음 단계를 검증한다.
- 이미지를 표시한 직후 다운로드 완료를 단정하던 테스트를 실제 로드 완료를 기다리도록 수정했다.
- Windows에서 실행 중인 Worker가 dist 폴더를 점유해 재빌드가 실패했다. 해당 검증 서버만 종료하고 다시 빌드하여 통과했다.
- Vinext의 ‘? /’ 정적 경로 분류 안내는 빌드 실패가 아니며 실제 빌드 서버에서 HTTP·Chromium 검증을 수행했다.

## 재실행

```powershell
npm run typecheck
npm run lint
npm run build
# 별도 터미널에서 빌드 Worker 시작
npm run start -- --port 5174

$env:TEST_BASE_URL='http://127.0.0.1:5174'
$env:RUN_API_TESTS='1'
$env:RUN_LIVE_KAKAO_TESTS='1'
$env:RUN_CONNECTED_API_TESTS='1'
npm test
npm run test:e2e
```

실제 연결 검사는 해당 키와 DEMO_MODE=false가 필요하다. 기본 npm test는 외부 통합 검사를 건너뛴다. 재빌드할 때는 기존 빌드 검증 서버를 먼저 종료한다.

## 남은 범위

Google 해외 실제 계정 호출, 식당별 메뉴·가격·성분 공급 경로, 공인 건강 평가식, 지도 SDK·실제 경로시간, 계정·보호자 확인, 푸시는 완료 범위에 포함하지 않는다. 연결 API의 향후 가용성·한도는 이번 검증으로 보장하지 않는다.

## 2026-09-20 Vercel 이식 검증

- Next.js 16.3.4 / Node 24 빌드, 타입·정적 검사 통과.
- Neon Free 운영 DB에 버전·체크섬을 관리하는 마이그레이션 적용.
- 도메인·HTTP·DB·실제 외부 연동 98개 검사 중 전체 실행에서 97개 통과. OpenRouter 1개는 규칙 분석으로 대체되어 실패했으며, 별도 재검증에서 실제 OpenRouter 분석까지 통과했습니다. 일부 무료 공급자 호출은 여전히 규칙 분석으로 대체될 수 있습니다.
- Chromium 11개 사용자 흐름 통과. AI UI 검사는 결정적인 응답을 사용하고 실제 모델 호출은 HTTP 검사에서 별도 확인했습니다.
- 새 Postgres 검사: 밀리초 시간·매개변수 보존, 실패 시 전체 롤백, 동시 식사 중복 방지, 세션 삭제 시 식사 삭제.
- 새 요청 출처 검사: Next.js 내부 localhost 주소와 외부 Host 구분, Vercel HTTPS·보안 쿠키, 전달 헤더 변조와 잘못된 Host 거부. 기존 CSRF·세션 소유권 검사 통과.
- 업로드 대상 209개 파일과 브라우저 자산 12개에서 API 키 4개·DB 접속 문자열·DB 비밀번호의 실제 값 포함 여부를 검사해 0건 확인. .env와 로컬 DB·도구 출력은 업로드 대상에서 제외.

- 런타임 모듈은 Node.js 표준 package imports와 Vite 별칭으로 분리했습니다. 기존 로컬 D1 서버에서도 추천 → 중복 확인 → 평가 → 삭제 회귀 검증 1건을 통과했습니다.
