# 구현 결정

## 명세 해석

Part I은 제품 요구사항이다. Part II는 앱 내부 런타임 프롬프트이며 개발 도구의 권한을 확장하지 않는다. 사용자 요청에 따라 로컬 소스와 실행 가능한 데모를 완성했다. 공개 배포·계정 생성·유료 서비스 활성화·저장소 업로드는 수행하지 않았다.

자세한 분석과 단계는 [구현 계획](docs/IMPLEMENTATION_PLAN.md), 합의 33개와 시나리오 50개 대응은 [요구사항표](docs/REQUIREMENTS.md)에 있다.

## 구조

React 19 / TypeScript / Vinext / Vite / Worker 호환 API / D1(SQLite)를 사용한다. Sites starter의 빌드·컴포넌트 기반과 잠금 파일을 유지하고 순수 도메인, provider, 서버, 저장소, 기능 화면으로 분리했다.

화면은 순위·안전 판정을 재계산하지 않는다. 외부 I/O는 provider, SQL은 repository 경계를 사용한다. `lunch-app.tsx`는 사용자 동작의 연결을 맡고 각 화면은 별도 파일이다. 출처 상태, 기본 설정, 오늘 조건, 학습, 실제 식사를 각각 구분한다.

## 데이터가 없을 때의 선택

- 예산·반경·시간은 null. 입력하지 않은 값을 사용자가 정한 값처럼 취급하지 않는다.
- 알레르기 기본값은 unknown. 없음/모름/미입력/제공을 구분한다.
- 건강 적합도는 null. 검증된 평가식과 필요한 영양 자료 없이는 숫자 점수를 만들지 않는다.
- 기록 없는 날은 null이며 결식·0점이 아니다.
- 실제 API에 메뉴가 없으면 주변 장소만 반환한다.
- 정확히 2곳만 적격이면 2곳만 표시한다. 조건부 후보는 별도 영역이다.

## 제품 기본값과 휴리스틱

| 항목             | 선택                                                                    |
| ---------------- | ----------------------------------------------------------------------- |
| 이름·시각 디자인 | 한끼로그, 짙은 녹색·밝은 작업 공간, 조건·추천 중심                      |
| 중요도           | 내부 0..1, 기본 0.6씩 동일, 표시 5점                                    |
| 피드백           | C 기본, 평점만 제출 가능, B 선택 시 이유 없음                           |
| 반복             | 취향 항목에서 동일 메뉴 회당 6, 최대 12 감점; 명시적 요청이면 감점 없음 |
| 맛 학습          | 동의한 실제 식사의 명시적 맛 평가만, 항목당 ±8 이내                     |
| 그룹             | 개인별 가능한 메뉴, 평균 + 강한 비선호 보호; 임계 35, 계수 0.35         |
| 큰 변경 제안     | 정책 함수만: 최소 10회, 14일 간격, 거절 후 30일. 실제 제안 UI 비활성    |
| 보관             | 식사 30일 기본, 선택 7/30/90일, 세션 수명 이내                          |
| 알림             | OFF, 시각 11:30·월–금·Asia/Seoul은 희망 설정만 보관                     |

이 계수는 제품 기본값이며 과학적 영양 기준이나 검증된 확률이 아니다. 개인 설정과 원 공식 기준은 분리한다. 안내 강도·관심 항목은 저장되지만 별도 문구 생성은 아직 구현하지 않았다.

## 개인정보와 그룹

선택적 동의는 기본 OFF다. 세션은 HttpOnly 난수 쿠키의 해시를 사용하며 demo/live 환경을 구분한다. 민감 프로필은 보관 OFF일 때 DB에서 제거한다. 보관 ON은 별도 동의+AES-GCM 키를 요구한다.

초대는 24시간이며 원 조건은 비공개다. 동행인 간단 입력에는 건강정보를 넣을 수 없다. 설정·동의·멤버 구성이 바뀌면 해당 그룹의 임시 추천·대기 선택을 무효화한다. 사용자가 저장한 설정 변경은 기존 그룹 공유를 종료한다.

## 정직한 미지원 범위

공식 식품 영양값·행정정보·동의 기반 OpenRouter 식사 구분·Kakao 조회는 연결하고 실제 호출을 검증했다. 실제 식당 메뉴 수집, 건강 평가식, 경로·지도 SDK, 계정·보호자 확인, 앱 종료 후 알림은 미연결이다. Google은 키가 없어 실제 검증하지 않았다. 연결 화면은 키 설정과 지원 범위를 표시하며 상시 가용성을 보장하지 않는다.

시연에서 유효한 식당과 흐름은 실제 서버·SQLite·브라우저로 검증했다. 이 검증은 서비스 공개 운영이나 의료적 안전 인증을 대신하지 않는다.
